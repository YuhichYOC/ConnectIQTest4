class IconPrinter {

    private var containerWidth;
    private var containerHeight;
    private var icons;

    public function init(cw, ch) {
        containerWidth = cw;
        containerHeight = ch;
    }

    public function print(l) {
        if (l.success()) {
        }
    }

}